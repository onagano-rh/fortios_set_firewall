fortios_set_firewall
=========

Test with tox and ansible-lint
----------------

Install `tox` first:

    pip install --user tox

Then execute it without any arguments:

    tox

It creates a virtualenv and execute `molecule lint` internally.
